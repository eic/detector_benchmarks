sim:nhcal_dimuon_fotoproduction:
  extends: .det_benchmark
  stage: simulate
  parallel:
    matrix:
      - INDEX_RANGE: ["0 4","5 9"]
  script:
    - snakemake $SNAKEMAKE_FLAGS --cores $MAX_CORES_PER_JOB --restart-times 2 $(for INDEX in $(seq -f '%02.0f' $INDEX_RANGE); do echo sim_output/nhcal_dimuon_fotoproduction/sim_epic_full.${INDEX}.edm4hep.root; done)

bench:nhcal_dimuon_fotoproduction_analysis:
  extends: .det_benchmark
  stage: benchmarks
  needs:
    - "sim:nhcal_dimuon_fotoproduction"
  script:
    - snakemake $SNAKEMAKE_FLAGS --cores $MAX_CORES_PER_JOB --restart-times 2 results/nhcal_dimuon_fotoproduction/analysis_epic_full_10files.pdf

collect_results:nhcal_dimuon_fotoproduction:
  extends: .det_benchmark
  stage: collect
  needs: 
    - "bench:nhcal_dimuon_fotoproduction_analysis"
  script:
    - ls -lrht
    - mv results{,_save}/ 
    - snakemake $SNAKEMAKE_FLAGS --cores 1 --delete-all-output results/nhcal_dimuon_fotoproduction/analysis_epic_full_10files.pdf
    - mv results{_save,}/
