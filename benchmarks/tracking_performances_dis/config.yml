sim:tracking_performances_dis:
  extends: .det_benchmark
  stage: simulate
  script:
    - snakemake --cache --cores 10 results/tracking_performances_dis/epic_craterlake_tracking_only/pythia8NCDIS_18x275_minQ2=1_combined_10/hists.root 
  retry:
    max: 2
    when:
      - runner_system_failure

bench:tracking_performances_dis:
  extends: .det_benchmark
  stage: benchmarks
  needs:
    - ["sim:tracking_performances_dis"]
  script:
    - snakemake --cores 1 results/tracking_performances_dis/epic_craterlake_tracking_only/pythia8NCDIS_18x275_minQ2=1_combined_10/plots.pdf 

collect_results:tracking_performances_dis:
  extends: .det_benchmark
  stage: collect
  needs:
    - "bench:tracking_performances_dis"
  script:
    - ls -lrht
    - mv results{,_save}/ # move results directory out of the way to preserve it
    - snakemake --cores 1 --delete-all-output tracking_performances_dis
    - mv results{_save,}/
